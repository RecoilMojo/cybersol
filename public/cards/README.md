# Card art

`cards.json` defines the Neon Grid starter pool (stats + accent colors).

Optional: drop WebP/PNG files named `{id}.webp` here (max ~512px) and wire them into `CardView` later. MVP renders gradient cards from `color` so the download stays tiny.
